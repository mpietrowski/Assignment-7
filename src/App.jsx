import React from 'react';

// Brings in the Parent component
import Parent from './Parent';

function App() {

// Main app component and loads the Parent component onto the page
return (
    <div>
    <Parent></Parent>
    </div>
  );
}

// Makes this usable in other files
export default App;